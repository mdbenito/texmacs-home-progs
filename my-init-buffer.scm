; hey

;(define MasterArbeitDir "/Users/miguel/Documents/Universidad/Two sided contact")

;(if (== (dirname (url->string (current-buffer-url))) MasterArbeitDir)
;    (kbd-map
;      (:mode in-math?)
;      ("macos i" (begin (make-script #t #f) (insert "<iota>") (go-right)))))

;(if (url-scratch? (current-buffer))
;    (begin
;      (set-style-list (append (get-style-list) (list "Snippet")))
;      (buffer-pretend-saved (current-buffer))))
    
